services:
  # L'API Python (Le Cerveau)
  cv-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cv-finaxys-api
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ./vector_db:/app/vector_db
      - ./logs:/app/logs
      - ./input:/app/input
    environment:
      - DISABLE_DOCLING=1
    env_file: .env
    restart: always

  # Le Bot Telegram (même image que cv-api, commande différente)
  cv-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cv-finaxys-bot
    command: python bot.py
    volumes:
      - ./output:/app/output
      - ./vector_db:/app/vector_db
      - ./logs:/app/logs
      - ./input:/app/input
    environment:
      - DISABLE_HEALTHCHECK=true
    env_file: .env
    depends_on:
      - cv-api
    restart: always
