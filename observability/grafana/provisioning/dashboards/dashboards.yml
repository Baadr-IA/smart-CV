apiVersion: 1

providers:
  - name: SmartCV
    orgId: 1
    folder: SmartCV
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
